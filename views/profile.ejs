<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title>Document</title>
  </head>
  <body class="bg-black">
    <div class="container py-5">
        <a class="btn btn-primary  text-white text-decoration-none  mb-4" href="/">Home</a>
        <a class="btn btn-danger  text-white text-decoration-none  mb-4 ms-3" href="/logout">Logout</a>
        <h2 class="text-white">Hello, <strong><%= user.username%></strong>👋</h2>
     <form action="/post" method="post">
        <h6 class="mt-3 text-secondary">You can create your post!</h6>
        <textarea class="w-25 " placeholder="What's on your mind?" name="content"></textarea><br>
        <input class="btn btn-primary text-center" type="submit" value="Post">
     </form>

        <h6 class="mt-5 text-secondary">Your posts are</h6>
        <%if(user.posts.length>0){%>
        <% user.posts.reverse().map((view)=>{ %>
            <div class="card bg-black mt-4 border p-3" style="width: 18rem;">
                <h5 class="card-title text-white">@<%=user.username%></h5>
                <p class="card-text text-secondary"><%=view.content%></p>
            <div>
                    <p class="text-secondary"><%=view.likes.length%> Likes</p>
                    <a href="/like/<%=view._id%>" class="text-info text-decoration-none"><%=view.likes.indexOf(user._id)?"Like" : "Unlike"%></a>
                    <a href="/edit/<%=view._id%>" class="ms-3 text-warning text-decoration-none">Edit</a>
                    <a href="/delete/<%=view._id%>" class="ms-3 text-danger text-decoration-none">Delete</a>
             </div>            
            </div>

        <% }) %>
        <%}else{%>
            <h6>No posts yet</h6>
        <%}%>
  
      </div>
  </body>
      <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
